{
  "name": "sentence-collector",
  "version": "2.12.5",
  "description": "A web console for collecting and validating sentences",
  "repository": {
    "type": "git",
    "url": "https://github.com/Common-Voice/sentence-collector.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "install:all": "cd server && npm ci && cd ../web && npm ci",
    "release": "semantic-release",
    "prepare": "husky install",
    "pull-cv-locales-info": "./scripts/pull-common-voice-locales-info.sh",
    "pull-cv-translations": "./scripts/pull-common-voice-translations.sh",
    "cleanup-cv-files": "git checkout locales && git clean -fxd web/locales && git checkout web/locales"
  },
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-conventional": "^13.1.0",
    "@semantic-release/commit-analyzer": "^8.0.1",
    "@semantic-release/git": "^9.0.0",
    "@semantic-release/github": "^7.2.3",
    "@semantic-release/npm": "^7.1.3",
    "@semantic-release/release-notes-generator": "^9.0.3",
    "husky": "^7.0.1",
    "semantic-release": "^17.4.4"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "body-max-line-length": [
        0,
        "always"
      ],
      "footer-max-line-length": [
        0,
        "always"
      ],
      "footer-leading-blank": [
        0,
        "always"
      ]
    }
  },
  "release": {
    "ci": false,
    "branches": [
      "main"
    ],
    "repositoryUrl": "git@github.com:Common-Voice/sentence-collector.git",
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/npm",
        {
          "npmPublish": false
        }
      ],
      [
        "@semantic-release/git",
        {
          "assets": [
            "package.json",
            "package-lock.json"
          ],
          "message": "chore(release): ${nextRelease.version}\n\n${nextRelease.notes}"
        }
      ],
      [
        "@semantic-release/github",
        {
          "failComment": false,
          "failTitle": false,
          "releasedLabels": false
        }
      ]
    ]
  }
}
